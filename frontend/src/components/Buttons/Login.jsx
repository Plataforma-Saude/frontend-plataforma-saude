export default function Login({ children }) {
    return (
        <button className="bg-white border-2 border-primary text-1xl flex justify-center items-center py-4 px-12 rounded-lg font-bold">{children}</button>
    )
}